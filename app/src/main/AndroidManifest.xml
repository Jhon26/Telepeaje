<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="co.edu.udea.telepeaje">

    <!-- PERMISOS -->
    <uses-permission android:name="android.permission.INTERNET" />
    //Internet
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    //Estado de la red
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    //Ubicacion fina
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    //Ubicacion no tan fina
    <uses-permission android:name="ANDROID.PERMISSION.WRITE_EXTERNAL_STORAGE" />
    //Escribir en el almacenamiento externo para no estar bajando datos repetidos de los mapas
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroEmailActivity" />
        <activity android:name=".InformacionPersonalActivity" />
        <activity android:name=".InformacionVehiculoActivity" />
        <activity android:name=".InformacionPagoActivity" />
        <activity android:name=".MisAutosActivity" />
        <activity android:name=".MiAutoActivity" />
        <activity android:name=".SeleccionarPagoActivity"></activity>
    </application>

</manifest>